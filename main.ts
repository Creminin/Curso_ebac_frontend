const Multiplicacao = (number1: number, number2: number): number => number1 * number2;

console.log(Multiplicacao(1, 2));

const Saudacao = (nome: string) => `Olá ${nome}`;

console.log(Saudacao('Nathan'))